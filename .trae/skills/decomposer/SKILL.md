---
name: "decomposer"
description: "Cognitive protocol that converts unknown unknowns into known unknowns. Invoke for large/ambiguous goals, or when user asks for honest breakdown, decomposition, or avoiding empty plans."
---

# Decomposer — TRAE Bridge

This is a TRAE discovery bridge. The actual TRAE adapter is at `adapters/trae/SKILL.md`. The universal skill definition is at `SKILL.md` (root). The full protocol is at `PROTOCOL.md`.

**For TRAE execution**: Follow the instructions in `adapters/trae/SKILL.md`. All references resolve relative to the project root.

> This file exists only because TRAE discovers skills at `.trae/skills/<name>/`. The canonical adapter lives at `adapters/trae/`.